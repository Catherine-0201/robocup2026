// Independent AMCL build wrapper.
//
// Bspline_guard_amcl.h declares the additional goal-guard interface used by
// the last B-spline version.  The implementation below is included verbatim
// from test_Bspline_guard.cpp, so its trajectory generation, tracking,
// velocity limiting, goal guarding, and arrival behavior are not forked or
// rewritten.

#include "Bspline_guard_amcl.h"
#include "test_Bspline_guard.cpp"
